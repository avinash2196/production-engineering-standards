# Plan — Example Feature

## Objective
Add an example read-only endpoint.

## Milestones
1. RED — define failing API behavior tests.
2. GREEN — implement the minimum endpoint behavior.
3. REFACTOR — only if justified after GREEN.

Human approval is required between phases.
