# Infrastructure layer — capability interface implementations.
# Sub-packages: messaging/, cache/, storage/, secrets/, fallback/
# Production implementations live here; fallback/ holds in-process substitutes.
