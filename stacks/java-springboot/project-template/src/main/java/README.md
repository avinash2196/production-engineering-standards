# Application Source Layout

This directory is intentionally source-free in the standards repository. An adopting project creates Java packages and classes only from its approved Implementation Plan rather than receiving speculative controllers, services, repositories, or infrastructure adapters by default.

Use the stack guidance in [`stacks/java-springboot/README.md`](../../../../README.md) and the repository's Prompt-Driven Development workflow when creating a real service.
