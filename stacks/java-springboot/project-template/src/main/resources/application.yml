spring:
  application:
    name: ${SERVICE_NAME:service-name}

server:
  port: ${PORT:8080}
  shutdown: graceful

# Add database, messaging, cache, storage, security, observability, and adapter
# configuration only when the approved Implementation Plan selects those capabilities.
# Local-only adapter values must be rejected when the service runs in production.
