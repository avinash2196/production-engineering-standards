# Local development profile — activate with: --spring.profiles.active=local
#
# This base profile deliberately defines no database, broker, cache, storage,
# security, or local-adapter defaults. Add only the local configuration selected
# by the approved Implementation Plan for the service being built.
#
# Do not commit real credentials.

logging:
  level:
    root: INFO
